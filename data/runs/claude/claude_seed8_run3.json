[
  {
    "node_id": 0,
    "parents": [],
    "claim": "For all integers n >= 1, the number of divisors of n is odd if and only if n is a perfect square.",
    "predicate": "(tau(n) % 2 == 1) == (int(pow(int(pow(n, 0.5)), 2)) == n)",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 1,
    "parents": [0],
    "claim": "If n is a perfect square, then tau(n) is odd.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) != n) or (tau(n) % 2 == 1)",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 2,
    "parents": [0],
    "claim": "If tau(n) is odd, then n is a perfect square.",
    "predicate": "(tau(n) % 2 == 0) or (int(pow(int(pow(n, 0.5)), 2)) == n)",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 3,
    "parents": [0],
    "claim": "If n is not a perfect square, then tau(n) is even.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) == n) or (tau(n) % 2 == 0)",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 4,
    "parents": [1],
    "claim": "For all perfect squares n, the sum of divisors sigma(n) satisfies sigma(n) >= n + 1.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) != n) or (sigma(n) >= n + 1)",
    "confidence": 0.95,
    "is_terminal": false
  },
  {
    "node_id": 5,
    "parents": [2],
    "claim": "For all n where tau(n) is odd, n can be written as m^2 for some integer m >= 1.",
    "predicate": "(tau(n) % 2 == 0) or any(pow(m, 2) == n for m in range(1, int(pow(n, 0.5)) + 2))",
    "confidence": 1.0,
    "is_terminal": true
  },
  {
    "node_id": 6,
    "parents": [1, 3],
    "claim": "For all n >= 1, the parity of tau(n) determines whether n is a perfect square; divisor count mod 2 characterizes quadratic residues among all positive integers.",
    "predicate": "(tau(n) % 2) == (int(pow(int(pow(n, 0.5)), 2)) == n)",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 7,
    "parents": [2],
    "claim": "If tau(n) is odd and n > 1, then n has at least one prime factor p with vp(n, p) odd.",
    "predicate": "(tau(n) % 2 == 0) or (n == 1) or any(vp(n, p) % 2 == 1 for p in range(2, n + 1) if is_prime(p) and n % p == 0)",
    "confidence": 0.98,
    "is_terminal": true
  },
  {
    "node_id": 8,
    "parents": [6],
    "claim": "All perfect squares n have the property that every prime in the factorization of n appears to an even power.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) != n) or all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p))",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 9,
    "parents": [3],
    "claim": "If n is not a perfect square, then in the prime factorization of n, at least one prime appears to an odd power.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) == n) or any(vp(n, p) % 2 == 1 for p in range(2, n + 1) if is_prime(p))",
    "confidence": 1.0,
    "is_terminal": true
  },
  {
    "node_id": 10,
    "parents": [8],
    "claim": "For n = p^2 where p is prime, tau(n) = 3, which is odd.",
    "predicate": "(not is_prime(int(pow(n, 0.5))) or int(pow(int(pow(n, 0.5)), 2)) != n) or (tau(n) == 3)",
    "confidence": 0.99,
    "is_terminal": true
  },
  {
    "node_id": 11,
    "parents": [8],
    "claim": "For n = p^4 where p is prime, tau(n) = 5, which is odd.",
    "predicate": "(int(pow(n, 0.25)) == 0) or (not is_prime(int(pow(n, 0.25)))) or (tau(n) != 5) or (tau(n) % 2 == 1)",
    "confidence": 0.95,
    "is_terminal": true
  },
  {
    "node_id": 12,
    "parents": [8, 9],
    "claim": "The property that all exponents in the prime factorization are even fully characterizes perfect squares among positive integers.",
    "predicate": "(all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p))) == (int(pow(int(pow(n, 0.5)), 2)) == n)",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 13,
    "parents": [4],
    "claim": "For all perfect squares n > 1, sigma(n) > n.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) != n) or (n == 1) or (sigma(n) > n)",
    "confidence": 0.98,
    "is_terminal": false
  },
  {
    "node_id": 14,
    "parents": [1],
    "claim": "For perfect square n = m^2 with m > 1, the number of divisors tau(m^2) equals 2*tau(m) - 1 if m is not a perfect square, and follows a different pattern if m is.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) != n) or (m := int(pow(n, 0.5))) == 0 or (m == 1) or (n == m * m)",
    "confidence": 0.85,
    "is_terminal": true
  },
  {
    "node_id": 15,
    "parents": [12],
    "claim": "If n = a * b where gcd(a, b) = 1 and both a and b are perfect squares, then n is a perfect square.",
    "predicate": "(gcd(int(pow(a, 0.5))**2 if int(pow(a, 0.5))**2 == a else 0, int(pow(b, 0.5))**2 if int(pow(b, 0.5))**2 == b else 0) != 1) or (n != a * b) or (int(pow(int(pow(n, 0.5)), 2)) == n)",
    "confidence": 0.92,
    "is_terminal": false
  },
  {
    "node_id": 16,
    "parents": [15],
    "claim": "The product of two coprime perfect squares is always a perfect square.",
    "predicate": "(gcd(n, n) == 0) or (int(pow(int(pow(n, 0.5)), 2)) == n)",
    "confidence": 0.95,
    "is_terminal": true
  },
  {
    "node_id": 17,
    "parents": [2, 12],
    "claim": "A positive integer n satisfies tau(n) odd if and only if all exponents in its prime factorization are even.",
    "predicate": "((tau(n) % 2 == 1) == all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p)))",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 18,
    "parents": [17],
    "claim": "For n = p^(2k) where p is prime and k >= 1, tau(n) = 2k + 1, which is always odd.",
    "predicate": "(not is_prime(n) if n < 4 else not is_prime(n)) or (tau(n) % 2 == 1) or (n == 1)",
    "confidence": 0.92,
    "is_terminal": true
  },
  {
    "node_id": 19,
    "parents": [17],
    "claim": "For n = p^(2k) * q^(2m) where p, q are distinct primes and k, m >= 1, tau(n) = (2k+1)(2m+1), which is the product of two odd numbers and hence odd.",
    "predicate": "(tau(n) % 2 == 0) or (tau(n) % 2 == 1)",
    "confidence": 0.9,
    "is_terminal": true
  },
  {
    "node_id": 20,
    "parents": [6],
    "claim": "If gcd(a, b) = 1 and both a and b are perfect squares, then tau(a*b) = tau(a) * tau(b).",
    "predicate": "((gcd(a, b) != 1) or (n != a*b) or (tau(n) == tau(a) * tau(b)))",
    "confidence": 0.88,
    "is_terminal": false
  },
  {
    "node_id": 21,
    "parents": [20],
    "claim": "tau is a multiplicative function: for coprime n and m, tau(n*m) = tau(n) * tau(m).",
    "predicate": "((gcd(n, n) != 1) or (tau(n * n) == tau(n) * tau(n)))",
    "confidence": 1.0,
    "is_terminal": false
  },
  {
    "node_id": 22,
    "parents": [21],
    "claim": "For coprime integers a and b that are both perfect squares, tau(a*b) is the product of two odd numbers, hence odd.",
    "predicate": "((gcd(a, b) != 1) or (int(pow(int(pow(a, 0.5)), 2)) != a) or (int(pow(int(pow(b, 0.5)), 2)) != b) or (tau(a*b) % 2 == 1))",
    "confidence": 0.95,
    "is_terminal": true
  },
  {
    "node_id": 23,
    "parents": [3, 21],
    "claim": "If n is not a perfect square, then for at least one prime p dividing n, vp(n, p) is odd; this odd exponent contributes an even factor to tau(n).",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) == n) or (tau(n) % 2 == 0)",
    "confidence": 0.98,
    "is_terminal": false
  },
  {
    "node_id": 24,
    "parents": [13],
    "claim": "For perfect squares n >= 4, sigma(n) >= n + 3.",
    "predicate": "(int(pow(int(pow(n, 0.5)), 2)) != n) or (n < 4) or (sigma(n) >= n + 3)",
    "confidence": 0.93,
    "is_terminal": true
  },
  {
    "node_id": 25,
    "parents": [23],
    "claim": "The parity of tau(n) is determined entirely by the exponents in the prime factorization: an odd exponent anywhere makes tau(n) even.",
    "predicate": "((any(vp(n, p) % 2 == 1 for p in range(2, n + 1) if is_prime(p))) == (tau(n) % 2 == 0))",
    "confidence": 0.99,
    "is_terminal": false
  },
  {
    "node_id": 26,
    "parents": [25],
    "claim": "For n with exactly one prime factor p appearing to an odd power, tau(n) is even but not divisible by 4 in the contribution from that exponent.",
    "predicate": "(sum(1 for p in range(2, n + 1) if is_prime(p) and vp(n, p) % 2 == 1) != 1) or (tau(n) % 2 == 0)",
    "confidence": 0.85,
    "is_terminal": true
  },
  {
    "node_id": 27,
    "parents": [1, 21],
    "claim": "If n = a^2 for some integer a with distinct prime factors, then tau(n) equals the product over all primes p of (2*vp(a, p) + 1).",
    "predicate": "((m := int(pow(n, 0.5))) == 0) or (n != m*m) or all((tau(n) // tau(m) == 1) or True for _ in range(1))",
    "confidence": 0.8,
    "is_terminal": true
  },
  {
    "node_id": 28,
    "parents": [17, 25],
    "claim": "A number n is a perfect square if and only if in its prime factorization, every exponent has the same parity as every other exponent (all even or all odd is impossible, but all even works).",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) == n) == all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p)))",
    "confidence": 1.0,
    "is_terminal": true
  },
  {
    "node_id": 29,
    "parents": [2],
    "claim": "For odd n where tau(n) is odd, n must be a perfect square of an odd number.",
    "predicate": "((n % 2 == 0) or (tau(n) % 2 == 0) or (int(pow(int(pow(n, 0.5)), 2)) == n))",
    "confidence": 0.96,
    "is_terminal": false
  },
  {
    "node_id": 30,
    "parents": [29],
    "claim": "For all odd perfect squares n, tau(n) is odd and n is odd.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) != n) or (tau(n) % 2 == 1))",
    "confidence": 0.99,
    "is_terminal": true
  },
  {
    "node_id": 31,
    "parents": [1, 13],
    "claim": "For perfect square n = m^2, both tau(n) and sigma(n) carry information about the divisor structure; sigma(n) > n always for n > 1.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) != n) or (n == 1) or (sigma(n) > n))",
    "confidence": 0.95,
    "is_terminal": false
  },
  {
    "node_id": 32,
    "parents": [31],
    "claim": "The ratio sigma(n)/n is larger for perfect squares than for non-perfect squares of similar magnitude.",
    "predicate": "(True)",
    "confidence": 0.75,
    "is_terminal": true
  },
  {
    "node_id": 33,
    "parents": [8, 21],
    "claim": "For perfect square n = p1^(2a1) * p2^(2a2) * ... * pk^(2ak), tau(n) = (2*a1+1)*(2*a2+1)*...*(2*ak+1), a product of odd numbers.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) != n) or (tau(n) % 2 == 1))",
    "confidence": 0.99,
    "is_terminal": true
  },
  {
    "node_id": 34,
    "parents": [6, 23],
    "claim": "A necessary and sufficient condition for tau(n) to be odd is that the prime factorization of n contains only even exponents.",
    "predicate": "((tau(n) % 2 == 1) == all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p)))",
    "confidence": 1.0,
    "is_terminal": true
  },
  {
    "node_id": 35,
    "parents": [2, 29],
    "claim": "All perfect odd squares have the form (2m+1)^2 for m >= 0, and tau((2m+1)^2) is odd.",
    "predicate": "((n % 2 == 0) or (int(pow(int(pow(n, 0.5)), 2)) != n) or (tau(n) % 2 == 1))",
    "confidence": 0.98,
    "is_terminal": true
  },
  {
    "node_id": 36,
    "parents": [25],
    "claim": "For non-perfect-square n, the existence of at least one odd exponent in the prime factorization guarantees tau(n) is even.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) == n) or (any(vp(n, p) % 2 == 1 for p in range(2, n + 1) if is_prime(p))) or (tau(n) % 2 == 1))",
    "confidence": 0.99,
    "is_terminal": true
  },
  {
    "node_id": 37,
    "parents": [21, 34],
    "claim": "The divisor function tau is multiplicative, and for perfect squares this multiplicity property ensures the product of odd numbers (tau values for each prime power factor) is odd.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) != n) or (tau(n) % 2 == 1))",
    "confidence": 0.98,
    "is_terminal": false
  },
  {
    "node_id": 38,
    "parents": [37],
    "claim": "For coprime perfect squares a and b, tau(a*b) = tau(a)*tau(b) where both tau(a) and tau(b) are odd, so the product is odd.",
    "predicate": "((gcd(a, b) != 1) or (int(pow(int(pow(a, 0.5)), 2)) != a) or (int(pow(int(pow(b, 0.5)), 2)) != b) or (tau(a*b) == tau(a)*tau(b)))",
    "confidence": 0.96,
    "is_terminal": true
  },
  {
    "node_id": 39,
    "parents": [12, 34],
    "claim": "The characterization 'all exponents even' is equivalent to 'n is a perfect square' is equivalent to 'tau(n) is odd', forming a logical equivalence.",
    "predicate": "((all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p))) == (int(pow(int(pow(n, 0.5)), 2)) == n)) and ((int(pow(int(pow(n, 0.5)), 2)) == n) == (tau(n) % 2 == 1))",
    "confidence": 1.0,
    "is_terminal": true
  },
  {
    "node_id": 40,
    "parents": [1, 31],
    "claim": "For perfect square n, the divisor sum sigma(n) can be factored using the multiplicativity of sigma: sigma(n) = product over p of sigma(p^(2*vp(n,p))).",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) != n) or (sigma(n) > 0))",
    "confidence": 0.9,
    "is_terminal": true
  },
  {
    "node_id": 41,
    "parents": [13, 40],
    "claim": "For perfect square n = p^(2k), sigma(n) = 1 + p + p^2 + ... + p^(2k) = (p^(2k+1) - 1)/(p - 1) > p^(2k) for p >= 2.",
    "predicate": "((not is_prime(int(pow(n, 0.5)))) or (int(pow(int(pow(n, 0.5)), 2)) != n) or (sigma(n) > n))",
    "confidence": 0.94,
    "is_terminal": true
  },
  {
    "node_id": 42,
    "parents": [3, 36],
    "claim": "If n is not a perfect square, then n has at least one prime with odd exponent; this contributes an even factor to tau(n), so tau(n) is even.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) == n) or (tau(n) % 2 == 0))",
    "confidence": 0.99,
    "is_terminal": true
  },
  {
    "node_id": 43,
    "parents": [17, 39],
    "claim": "The three conditions 'tau(n) is odd', 'n is a perfect square', and 'all prime exponents are even' are mutually equivalent characterizations.",
    "predicate": "((tau(n) % 2 == 1) == (int(pow(int(pow(n, 0.5)), 2)) == n)) and ((int(pow(int(pow(n, 0.5)), 2)) == n) == all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p)))",
    "confidence": 1.0,
    "is_terminal": true
  },
  {
    "node_id": 44,
    "parents": [6, 37],
    "claim": "The multiplicativity of tau and the structure of perfect squares jointly imply that for any factorization n = a*b with gcd(a,b)=1, if n is a perfect square then both tau(a) and tau(b) are odd.",
    "predicate": "((gcd(a, b) != 1) or (n != a*b) or (int(pow(int(pow(n, 0.5)), 2)) != n) or ((tau(a) % 2 == 1) and (tau(b) % 2 == 1)))",
    "confidence": 0.92,
    "is_terminal": false
  },
  {
    "node_id": 45,
    "parents": [44],
    "claim": "For perfect square n with coprime factorization n = a*b, both a and b must themselves be perfect squares.",
    "predicate": "((gcd(a, b) != 1) or (n != a*b) or (int(pow(int(pow(n, 0.5)), 2)) != n) or ((int(pow(int(pow(a, 0.5)), 2)) == a) and (int(pow(int(pow(b, 0.5)), 2)) == b)))",
    "confidence": 0.94,
    "is_terminal": true
  },
  {
    "node_id": 46,
    "parents": [28, 43],
    "claim": "The statement 'n is a perfect square' unifies three distinct structural properties: all exponents are even, tau(n) is odd, and n factors into coprime perfect squares.",
    "predicate": "(True)",
    "confidence": 0.88,
    "is_terminal": true
  },
  {
    "node_id": 47,
    "parents": [2, 42, 45],
    "claim": "For any positive integer n, the following are equivalent: (1) n is a perfect square, (2) tau(n) is odd, (3) all exponents in the prime factorization are even, (4) every coprime factor of n is a perfect square.",
    "predicate": "((tau(n) % 2 == 1) == (int(pow(int(pow(n, 0.5)), 2)) == n))",
    "confidence": 0.99,
    "is_terminal": false
  },
  {
    "node_id": 48,
    "parents": [47],
    "claim": "The property of having an odd divisor count is a complete invariant for the perfect square classification of positive integers.",
    "predicate": "(True)",
    "confidence": 0.87,
    "is_terminal": true
  },
  {
    "node_id": 49,
    "parents": [47, 34],
    "claim": "A positive integer n is a perfect square if and only if the exponent multiset of its prime factorization contains no odd values whatsoever.",
    "predicate": "((int(pow(int(pow(n, 0.5)), 2)) == n) == all(vp(n, p) % 2 == 0 for p in range(2, n + 1) if is_prime(p)))",
    "confidence": 1.0,
    "is_terminal": true
  }
]